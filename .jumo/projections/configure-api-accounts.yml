apiVersion: jumo.dev/v1
kind: ProjectionSpec
metadata:
  id: configure-api-accounts
  namespace: dev.jumo.core
  name: Configure declared API accounts
spec:
  ownerRealm: home
  payloadSchemaRef: configure-api-accounts
  projectionKind: FORM
  renderedBy: home
  actions: [provider.account.observe]
  sections:
    - id: accounts
      i18nKey: journeyApiAccountsSection
      columns: 1
      fields:
        - path: providerAccountRefs
          representation: ENTITY_COLLECTION
          optionsFrom: ProviderAccount
          optionsEligibility:
            - path: spec.routing.mode
              equalsValue: GATEWAY_ROUTED
            - path: spec.secretBindingRef
              present: true
          i18nKey: journeyProviderAccountsField
          required: true
