// GENERATED by scripts/generate-metamodel.sh from metamodel/jumo-v1.yaml -- do not edit. Edit the LinkML source and regenerate.
package dev.jumo.model;

/* metamodel_version: 1.11.0 */
import java.util.Map;
import java.util.HashMap;
import java.util.Collections;

import com.fasterxml.jackson.annotation.JsonCreator;

/**
 * How a result or an AttentionItem reaches the human. Shared by Practice and AttentionItem so a rhythm and the items it carries cannot describe delivery in two vocabularies. Pausing delivery never deletes the canonical event.
 */
public enum DeliveryMode {

  LIVE("LIVE"),

  DIGEST("DIGEST"),

  ESCALATION_ONLY("ESCALATION_ONLY"),

  DELEGATED("DELEGATED"),

  MUTED_UNTIL("MUTED_UNTIL"),

  QUIET_HOURS("QUIET_HOURS");

  private final static Map<String, DeliveryMode> MAP;

  static {
    Map<String, DeliveryMode> map = new HashMap<String, DeliveryMode>();
    for ( DeliveryMode value : DeliveryMode.values() ) {
      map.put(value.toString(), value);
    }

    MAP = Collections.unmodifiableMap(map);
  }

  private final String repr;

  DeliveryMode(String repr) {
    this.repr = repr;
  }

  @Override
  public String toString() {
    return repr;
  }

  @JsonCreator
  public static DeliveryMode fromString(String v) {
    return MAP.get(v);
  }

}