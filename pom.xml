<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>dev.jumo</groupId>
  <artifactId>jumo-core-reactor</artifactId>
  <version>1</version>
  <packaging>pom</packaging>
  <name>Jumo Core :: build reactor</name>
  <description>
    jumo-core is the declarative PLATFORM corpus (.jumo, metamodel, policy, docs) -- it carries no
    application code. This reactor exists for one reason: metamodel/jumo-v1.yaml owns the Java
    records jumo depends on, so the module that generates and packages them, dev.jumo:jumo-model,
    has to build and publish from wherever the metamodel it is generated from actually lives
    (metamodel-and-policy-to-core AC2). Nothing else in jumo-core is a Maven artifact.
  </description>

  <modules>
    <module>modules/jumo-model</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>21</maven.compiler.release>
    <jackson2.version>2.21</jackson2.version>
  </properties>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.14.1</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>3.5.1</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-install-plugin</artifactId>
          <version>3.1.4</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-deploy-plugin</artifactId>
          <version>3.1.4</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <distributionManagement>
    <repository>
      <id>github</id>
      <name>GitHub Packages -- jumo-core</name>
      <url>https://maven.pkg.github.com/open4good/jumo-core</url>
    </repository>
  </distributionManagement>
</project>
