// GENERATED by scripts/generate-metamodel.sh from metamodel/jumo-v1.yaml -- do not edit. Edit the LinkML source and regenerate.
package dev.jumo.model;

/* metamodel_version: 1.11.0 */
import java.util.Map;
import java.util.HashMap;
import java.util.Collections;

import com.fasterxml.jackson.annotation.JsonCreator;

/**
 * Confidentiality dimension, split out from the single DataScope enum: classification, ownership, and declassification are distinct concerns (docs/concepts/positionnement-conceptuel.md#flux-dinformation). Used where an explicit cross-Realm release transformation is modeled, alongside ReleaseTransformation and ReleaseCompartment below -- not a replacement for DataScope's simpler publication-boundary role.
 */
public enum InformationClassification {

  PUBLIC("PUBLIC"),

  INTERNAL("INTERNAL"),

  REALM_CONFIDENTIAL("REALM_CONFIDENTIAL"),

  SECRET("SECRET");

  private final static Map<String, InformationClassification> MAP;

  static {
    Map<String, InformationClassification> map = new HashMap<String, InformationClassification>();
    for ( InformationClassification value : InformationClassification.values() ) {
      map.put(value.toString(), value);
    }

    MAP = Collections.unmodifiableMap(map);
  }

  private final String repr;

  InformationClassification(String repr) {
    this.repr = repr;
  }

  @Override
  public String toString() {
    return repr;
  }

  @JsonCreator
  public static InformationClassification fromString(String v) {
    return MAP.get(v);
  }

}