// GENERATED by scripts/generate-metamodel.sh from metamodel/jumo-v1.yaml -- do not edit. Edit the LinkML source and regenerate.
package dev.jumo.model;

/* metamodel_version: 1.11.0 */
import java.util.Map;
import java.util.HashMap;
import java.util.Collections;

import com.fasterxml.jackson.annotation.JsonCreator;

/**
 * UNTRUSTED carries content originating outside the trust boundary -- email bodies, web pages, transcripts, inbound channel messages. This is propagated provenance metadata, not by itself a security boundary (docs/concepts/positionnement-conceptuel.md#separation-instructions-donnees): the runtime fences these and never treats them as instructions, but authorization decisions are made on concrete argument values, not on this tag alone.
 */
public enum VariableTrust {

  TRUSTED("TRUSTED"),

  UNTRUSTED("UNTRUSTED");

  private final static Map<String, VariableTrust> MAP;

  static {
    Map<String, VariableTrust> map = new HashMap<String, VariableTrust>();
    for ( VariableTrust value : VariableTrust.values() ) {
      map.put(value.toString(), value);
    }

    MAP = Collections.unmodifiableMap(map);
  }

  private final String repr;

  VariableTrust(String repr) {
    this.repr = repr;
  }

  @Override
  public String toString() {
    return repr;
  }

  @JsonCreator
  public static VariableTrust fromString(String v) {
    return MAP.get(v);
  }

}