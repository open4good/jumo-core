// GENERATED by scripts/generate-metamodel.sh from metamodel/jumo-v1.yaml -- do not edit. Edit the LinkML source and regenerate.
package dev.jumo.model;

/* metamodel_version: 1.11.0 */
import java.util.Map;
import java.util.HashMap;
import java.util.Collections;

import com.fasterxml.jackson.annotation.JsonCreator;

/**
 * A worker's own structured signal about whether its answer, as given, is sufficient (worker-sufficiency-bounded-escalation AC1). Reported only when the declaring WorkerRequirementProfile's interaction.structuredOutput is REQUIRED.
 */
public enum ResponseSufficiency {

  SUFFICIENT("SUFFICIENT"),

  INSUFFICIENT_NEEDS_DEPTH("INSUFFICIENT_NEEDS_DEPTH");

  private final static Map<String, ResponseSufficiency> MAP;

  static {
    Map<String, ResponseSufficiency> map = new HashMap<String, ResponseSufficiency>();
    for ( ResponseSufficiency value : ResponseSufficiency.values() ) {
      map.put(value.toString(), value);
    }

    MAP = Collections.unmodifiableMap(map);
  }

  private final String repr;

  ResponseSufficiency(String repr) {
    this.repr = repr;
  }

  @Override
  public String toString() {
    return repr;
  }

  @JsonCreator
  public static ResponseSufficiency fromString(String v) {
    return MAP.get(v);
  }

}