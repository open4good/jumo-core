// GENERATED by scripts/generate-metamodel.sh from metamodel/jumo-v1.yaml -- do not edit. Edit the LinkML source and regenerate.
package dev.jumo.model;

/* metamodel_version: 1.11.0 */
import java.util.Map;
import java.util.HashMap;
import java.util.Collections;

import com.fasterxml.jackson.annotation.JsonCreator;

public enum CredentialSource {

  GATEWAY_TOKEN_ONLY("GATEWAY_TOKEN_ONLY"),

  PLAN_SESSION_ONLY("PLAN_SESSION_ONLY"),

  OPENBAO_RENDERED_FILE("OPENBAO_RENDERED_FILE");

  private final static Map<String, CredentialSource> MAP;

  static {
    Map<String, CredentialSource> map = new HashMap<String, CredentialSource>();
    for ( CredentialSource value : CredentialSource.values() ) {
      map.put(value.toString(), value);
    }

    MAP = Collections.unmodifiableMap(map);
  }

  private final String repr;

  CredentialSource(String repr) {
    this.repr = repr;
  }

  @Override
  public String toString() {
    return repr;
  }

  @JsonCreator
  public static CredentialSource fromString(String v) {
    return MAP.get(v);
  }

}