apiVersion: jumo.dev/v1
kind: ProjectContract
metadata:
  id: jumo
  namespace: dev.jumo.core
spec:
  purpose: Git-governed control plane for delegated agency
  primaryLanguage: java
  deployment: docker-compose
  runtime:
    durableWorkflowEngine: temporal
  authority:
    desiredState: git
    businessState: postgresql
    executionState: temporal
  documentation:
    canonicalDecisions: docs/00-canonical-decisions.md
    handoff: docs/operations/runtime.md
