apiVersion: jumo.dev/v1
kind: AgentDefinition
metadata:
  id: federating-chief-of-staff
  namespace: dev.jumo.core
  defaultDisplayName: Nestor
spec:
  responsibilities:
    - make uncertainty visible in a bounded work order
    - draft a bounded ChangeProposal
    - route accepted work to the declared implementer or reviewer
  prohibitedAuthority:
    - self_grant_capability
    - bypass_policy
    - infer_transitive_access
    - self_approve_privilege_change
    - modify_root_of_trust
    - act_as_super_administrator
  requestedCapabilities:
    - repository.content.read
    - contract.change.propose
    - work.order.propose
    # The six read capabilities the platform-operations/core.yml catalogue names. An
    # operation is offerable to this agent only where it already requests that capability
    # (corpus.platform-operation.exposure-requested), so this list is what bounds the tool
    # catalogue -- exposure is never a second way to obtain authority.
    - work.order.read
    - connector.read
    - mcp.catalog.read
    - knowledge.search.request
    - team.roster.read
    - interface.manifest.read
  defaultAutonomy: SUPERVISED
  modificationRing: RING_2_AGENT_BEHAVIOR
