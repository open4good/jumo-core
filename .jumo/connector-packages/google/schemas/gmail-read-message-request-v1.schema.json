{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://jumo.dev/schemas/connector-packages/google-workspace/gmail-read-message-request-v1",
  "type": "object",
  "properties": {
    "messageId": {
      "type": "string",
      "minLength": 1,
      "description": "A message id previously returned by gmail-search. Reading an id that call did not return is refused -- explicit owner selection only, never a free-text lookup."
    }
  },
  "required": ["messageId"],
  "additionalProperties": false
}
