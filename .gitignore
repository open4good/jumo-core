# Eclipse project metadata is local tooling state, never a Platform contract.
.project

# --- Build (metamodel-and-policy-to-core AC2: jumo-model is a real Maven module now) ---------
target/
*.jar

# Published derived projection; CI regenerates it only when releasing its versioned artifact.
metamodel/generated/

# --- Tooling -----------------------------------------------------------------
.venv/
__pycache__/
*.py[cod]

# --- IDE ---------------------------------------------------------------------
.idea/
.vscode/
.classpath
.settings/
*.iml
