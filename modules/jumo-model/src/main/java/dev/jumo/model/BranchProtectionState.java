// GENERATED by scripts/generate-metamodel.sh from metamodel/jumo-v1.yaml -- do not edit. Edit the LinkML source and regenerate.
package dev.jumo.model;

/* metamodel_version: 1.11.0 */
import java.util.Map;
import java.util.HashMap;
import java.util.Collections;

import com.fasterxml.jackson.annotation.JsonCreator;

/**
 * ADVISORY means the Forge does not prevent a direct push, a force-push or a self-approved merge. A declaration is not a control -- it is a claim the owner makes.
 */
public enum BranchProtectionState {

  ADVISORY("ADVISORY"),

  ENFORCED("ENFORCED");

  private final static Map<String, BranchProtectionState> MAP;

  static {
    Map<String, BranchProtectionState> map = new HashMap<String, BranchProtectionState>();
    for ( BranchProtectionState value : BranchProtectionState.values() ) {
      map.put(value.toString(), value);
    }

    MAP = Collections.unmodifiableMap(map);
  }

  private final String repr;

  BranchProtectionState(String repr) {
    this.repr = repr;
  }

  @Override
  public String toString() {
    return repr;
  }

  @JsonCreator
  public static BranchProtectionState fromString(String v) {
    return MAP.get(v);
  }

}