// GENERATED by scripts/generate-metamodel.sh from metamodel/jumo-v1.yaml -- do not edit. Edit the LinkML source and regenerate.
package dev.jumo.model;

/* metamodel_version: 1.11.0 */
import java.util.Map;
import java.util.HashMap;
import java.util.Collections;

import com.fasterxml.jackson.annotation.JsonCreator;

/**
 * Ordered least to most intrusive. Separate from urgency on purpose: a critical item can still belong in the morning digest, and conflating the two is how everything urgent becomes an interruption.
 */
public enum AttentionInterruptibility {

  DEFER_TO_DIGEST("DEFER_TO_DIGEST"),

  MAY_INTERRUPT("MAY_INTERRUPT"),

  QUIET_HOURS_EXEMPT("QUIET_HOURS_EXEMPT");

  private final static Map<String, AttentionInterruptibility> MAP;

  static {
    Map<String, AttentionInterruptibility> map = new HashMap<String, AttentionInterruptibility>();
    for ( AttentionInterruptibility value : AttentionInterruptibility.values() ) {
      map.put(value.toString(), value);
    }

    MAP = Collections.unmodifiableMap(map);
  }

  private final String repr;

  AttentionInterruptibility(String repr) {
    this.repr = repr;
  }

  @Override
  public String toString() {
    return repr;
  }

  @JsonCreator
  public static AttentionInterruptibility fromString(String v) {
    return MAP.get(v);
  }

}