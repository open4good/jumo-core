// GENERATED by scripts/generate-metamodel.sh from metamodel/jumo-v1.yaml -- do not edit. Edit the LinkML source and regenerate.
package dev.jumo.model;

/* metamodel_version: 1.11.0 */
import java.util.Map;
import java.util.HashMap;
import java.util.Collections;

import com.fasterxml.jackson.annotation.JsonCreator;

/**
 * REFUSED is kept rather than deleted so a second submission of the same artifact meets the reasoning that rejected the first.
 */
public enum AppraisalVerdict {

  ACCEPTED("ACCEPTED"),

  ACCEPTED_WITH_CONDITIONS("ACCEPTED_WITH_CONDITIONS"),

  REFUSED("REFUSED");

  private final static Map<String, AppraisalVerdict> MAP;

  static {
    Map<String, AppraisalVerdict> map = new HashMap<String, AppraisalVerdict>();
    for ( AppraisalVerdict value : AppraisalVerdict.values() ) {
      map.put(value.toString(), value);
    }

    MAP = Collections.unmodifiableMap(map);
  }

  private final String repr;

  AppraisalVerdict(String repr) {
    this.repr = repr;
  }

  @Override
  public String toString() {
    return repr;
  }

  @JsonCreator
  public static AppraisalVerdict fromString(String v) {
    return MAP.get(v);
  }

}