// GENERATED by scripts/generate-metamodel.sh from metamodel/jumo-v1.yaml -- do not edit. Edit the LinkML source and regenerate.
package dev.jumo.model;


import java.util.List;
import java.util.List;
import java.util.Objects;

/**
 * Structured output of a DIALOGUE_COLLECT step capturing what the assistant proposes selecting, from a declared catalog, in response to the user's stated intent (AssistedJourneyStep.promptRef, PromptOutput form: STRUCTURED; PromptOutput.targetKind names the ContractReference kind every selectedRefs entry must be, e.g. ConnectorDefinition). A proposal, never a grant or a configuration: selectedRefs must be drawn only from the realm's declared catalog of that kind, supplied as the prompt's RAG context (the same corpus ContractLoader loads for CapabilityGatewayService), never invented by the model. The user still confirms selection explicitly; this class only bounds what the model may say back. One declaration reused across every domain that needs this shape, instead of one class per target kind (canonical decision 15).
 */
public record SelectionIntentProposal(List<ContractReference> selectedRefs, List<SelectionIntentRationale> rationale, String openQuestion)  {

    public static Builder builder() {
        return new Builder();
    }

    public static class Builder {

        private List<ContractReference> selectedRefs = List.of();
        private List<SelectionIntentRationale> rationale = List.of();
        private String openQuestion = "";


        public Builder selectedRefs(List<ContractReference> selectedRefs) {
            this.selectedRefs = Objects.requireNonNull(selectedRefs);
            return this;
        }

        public Builder rationale(List<SelectionIntentRationale> rationale) {
            this.rationale = Objects.requireNonNull(rationale);
            return this;
        }

        public Builder openQuestion(String openQuestion) {
            this.openQuestion = Objects.requireNonNull(openQuestion);
            return this;
        }

    public SelectionIntentProposal build() {
            return new SelectionIntentProposal(selectedRefs, rationale, openQuestion);
        }
    }
}
