// GENERATED by scripts/generate-metamodel.sh from metamodel/jumo-v1.yaml -- do not edit. Edit the LinkML source and regenerate.
package dev.jumo.model;

/* metamodel_version: 1.11.0 */
import java.util.Map;
import java.util.HashMap;
import java.util.Collections;

import com.fasterxml.jackson.annotation.JsonCreator;

/**
 * Recognized-state lifecycle of a resumable ActionRun (connector-action-runs-and-effect-batches AC1).
 */
public enum ActionRunStatus {

  PROPOSED("PROPOSED"),

  IN_PROGRESS("IN_PROGRESS"),

  AWAITING_APPROVAL("AWAITING_APPROVAL"),

  /**
   * At least one ActionStep resolved UNKNOWN; the run is paused pending reconciliation or an owner decision, never a blind retry (connector-action-runs-and-effect-batches AC3, not yet built).
   */
  PAUSED("PAUSED"),

  COMPLETED("COMPLETED"),

  FAILED("FAILED"),

  CANCELLED("CANCELLED");

  private final static Map<String, ActionRunStatus> MAP;

  static {
    Map<String, ActionRunStatus> map = new HashMap<String, ActionRunStatus>();
    for ( ActionRunStatus value : ActionRunStatus.values() ) {
      map.put(value.toString(), value);
    }

    MAP = Collections.unmodifiableMap(map);
  }

  private final String repr;

  ActionRunStatus(String repr) {
    this.repr = repr;
  }

  @Override
  public String toString() {
    return repr;
  }

  @JsonCreator
  public static ActionRunStatus fromString(String v) {
    return MAP.get(v);
  }

}