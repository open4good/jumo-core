// GENERATED by scripts/generate-metamodel.sh from metamodel/jumo-v1.yaml -- do not edit. Edit the LinkML source and regenerate.
package dev.jumo.model;

/* metamodel_version: 1.11.0 */
import java.util.Map;
import java.util.HashMap;
import java.util.Collections;

import com.fasterxml.jackson.annotation.JsonCreator;

/**
 * How a value crossed from its classification into its compartment.
 */
public enum ReleaseTransformation {

  VERBATIM("VERBATIM"),

  SUMMARIZED("SUMMARIZED"),

  REDACTED("REDACTED"),

  SANITIZED_REASON_CODE("SANITIZED_REASON_CODE");

  private final static Map<String, ReleaseTransformation> MAP;

  static {
    Map<String, ReleaseTransformation> map = new HashMap<String, ReleaseTransformation>();
    for ( ReleaseTransformation value : ReleaseTransformation.values() ) {
      map.put(value.toString(), value);
    }

    MAP = Collections.unmodifiableMap(map);
  }

  private final String repr;

  ReleaseTransformation(String repr) {
    this.repr = repr;
  }

  @Override
  public String toString() {
    return repr;
  }

  @JsonCreator
  public static ReleaseTransformation fromString(String v) {
    return MAP.get(v);
  }

}