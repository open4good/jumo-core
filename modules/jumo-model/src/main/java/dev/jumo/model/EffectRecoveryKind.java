// GENERATED by scripts/generate-metamodel.sh from metamodel/jumo-v1.yaml -- do not edit. Edit the LinkML source and regenerate.
package dev.jumo.model;

/* metamodel_version: 1.11.0 */
import java.util.Map;
import java.util.HashMap;
import java.util.Collections;

import com.fasterxml.jackson.annotation.JsonCreator;

/**
 * Replaces the source schemas' `reversible: boolean`: compensation is application-specific and can itself fail, so a single boolean does not say which of these applies (docs/concepts/positionnement-conceptuel.md#sagas-et-recuperation).
 */
public enum EffectRecoveryKind {

  RETRY("RETRY"),

  DEDUPLICATION("DEDUPLICATION"),

  COMPENSATION("COMPENSATION"),

  CORRECTION("CORRECTION"),

  RECONCILIATION("RECONCILIATION"),

  EXPIRY_WINDOW("EXPIRY_WINDOW"),

  RESIDUAL_HARM("RESIDUAL_HARM");

  private final static Map<String, EffectRecoveryKind> MAP;

  static {
    Map<String, EffectRecoveryKind> map = new HashMap<String, EffectRecoveryKind>();
    for ( EffectRecoveryKind value : EffectRecoveryKind.values() ) {
      map.put(value.toString(), value);
    }

    MAP = Collections.unmodifiableMap(map);
  }

  private final String repr;

  EffectRecoveryKind(String repr) {
    this.repr = repr;
  }

  @Override
  public String toString() {
    return repr;
  }

  @JsonCreator
  public static EffectRecoveryKind fromString(String v) {
    return MAP.get(v);
  }

}