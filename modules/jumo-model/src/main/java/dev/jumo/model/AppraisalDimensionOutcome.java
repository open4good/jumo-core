// GENERATED by scripts/generate-metamodel.sh from metamodel/jumo-v1.yaml -- do not edit. Edit the LinkML source and regenerate.
package dev.jumo.model;

/* metamodel_version: 1.11.0 */
import java.util.Map;
import java.util.HashMap;
import java.util.Collections;

import com.fasterxml.jackson.annotation.JsonCreator;

/**
 * CONCERN sits between PASS and FAIL so a reviewer can record something that did not block promotion without calling it clean.
 */
public enum AppraisalDimensionOutcome {

  PASS("PASS"),

  CONCERN("CONCERN"),

  FAIL("FAIL"),

  UNKNOWN("UNKNOWN");

  private final static Map<String, AppraisalDimensionOutcome> MAP;

  static {
    Map<String, AppraisalDimensionOutcome> map = new HashMap<String, AppraisalDimensionOutcome>();
    for ( AppraisalDimensionOutcome value : AppraisalDimensionOutcome.values() ) {
      map.put(value.toString(), value);
    }

    MAP = Collections.unmodifiableMap(map);
  }

  private final String repr;

  AppraisalDimensionOutcome(String repr) {
    this.repr = repr;
  }

  @Override
  public String toString() {
    return repr;
  }

  @JsonCreator
  public static AppraisalDimensionOutcome fromString(String v) {
    return MAP.get(v);
  }

}